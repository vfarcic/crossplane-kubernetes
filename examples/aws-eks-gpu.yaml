apiVersion: devopstoolkitseries.com/v2
kind: Cluster
metadata:
  name: a-team-gpu
spec:
  id: a-team-gpu
  crossplane:
    compositionSelector:
      matchLabels:
        provider: aws
        cluster: eks
  parameters:
    nodeSize: small
    minNodeCount: 1
    gpu:
      enabled: true
      nodeSize: small
