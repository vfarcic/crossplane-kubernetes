apiVersion: devopstoolkit.ai/v2
kind: Cluster
metadata:
  name: a-team-gpu
spec:
  crossplane:
    compositionSelector:
      matchLabels:
        provider: google
        cluster: gke
  parameters:
    nodeSize: small
    minNodeCount: 1
    gpu:
      enabled: true
      nodeSize: small
    apps:
      vllm:
        enabled: true
