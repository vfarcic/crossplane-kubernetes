apiVersion: devopstoolkit.ai/v2
kind: Cluster
metadata:
  name: a-team-gke
  namespace: a-team
spec:
  crossplane:
    compositionSelector:
      matchLabels:
        provider: google
        cluster: gke
  parameters:
    region: us-central1
    nodeSize: medium
    minNodeCount: 1
    namespaces:
      - dev
      - production
    gpu:
      enabled: true
      nodeSize: small
      minNodeCount: 1
    apps:
      llmd:
        enabled: true
