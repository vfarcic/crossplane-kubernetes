apiVersion: devopstoolkitseries.com/v2
kind: Cluster
metadata:
  name: a-team-aks
spec:
  id: a-team-aks
  crossplane:
    compositionSelector:
      matchLabels:
        provider: azure
        cluster: aks
  parameters:
    nodeSize: medium
    minNodeCount: 1
    version: 1.26.6
    apps:
      crossplane:
        enabled: true
