---
apiVersion: devopstoolkit.ai/v2
kind: Cluster
metadata:
  name: (join('-', ['a-team', $cluster]))
spec:
  parameters:
    apps:
      vllm:
        enabled: true
      envoyGateway:
        enabled: true
      prometheus:
        enabled: true
      keda:
        enabled: true
