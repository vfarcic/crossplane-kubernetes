---
apiVersion: devopstoolkitseries.com/v1alpha1
kind: ClusterClaim
metadata:
  name: (join('-', ['a-team', $cluster]))
spec:
  parameters:
    apps:
      argocd:
        enabled: true
        host: argocd.acme.com
