---
apiVersion: devopstoolkit.ai/v2
kind: Cluster
metadata:
  name: (join('-', ['a-team', $cluster]))
  namespace: ($namespace)
spec:
  parameters:
    apps:
      envoyGateway:
        enabled: true
  (crossplane.resourceRefs[?name == join('-', ['a-team', $cluster, 'app-ai-gateway-crds-helm'])]):
  - apiVersion: helm.m.crossplane.io/v1beta1
    kind: Release
  (crossplane.resourceRefs[?name == join('-', ['a-team', $cluster, 'app-ai-gateway-helm'])]):
  - apiVersion: helm.m.crossplane.io/v1beta1
    kind: Release
  (crossplane.resourceRefs[?name == join('-', ['a-team', $cluster, 'app-gateway-helm'])]):
  - apiVersion: helm.m.crossplane.io/v1beta1
    kind: Release
  (crossplane.resourceRefs[?name == join('-', ['a-team', $cluster, 'app-envoy-gateway-class'])]):
  - apiVersion: kubernetes.m.crossplane.io/v1alpha1
    kind: Object
  (crossplane.resourceRefs[?name == join('-', ['a-team', $cluster, 'app-envoy-gateway'])]):
  - apiVersion: kubernetes.m.crossplane.io/v1alpha1
    kind: Object
  (crossplane.resourceRefs[?name == join('-', ['a-team', $cluster, 'app-inference-crd-inferencepools'])]):
  - apiVersion: kubernetes.m.crossplane.io/v1alpha1
    kind: Object
  (crossplane.resourceRefs[?name == join('-', ['a-team', $cluster, 'app-inference-crd-inferenceobjectives'])]):
  - apiVersion: kubernetes.m.crossplane.io/v1alpha1
    kind: Object
---
apiVersion: helm.m.crossplane.io/v1beta1
kind: Release
metadata:
  annotations:
    crossplane.io/composition-resource-name: (join('-', ['a-team', $cluster, 'app-gateway-helm']))
    crossplane.io/external-name: gateway-helm
  name: (join('-', ['a-team', $cluster, 'app-gateway-helm']))
  ownerReferences:
  - apiVersion: devopstoolkit.ai/v2
    blockOwnerDeletion: true
    controller: true
    kind: Cluster
spec:
  forProvider:
    chart:
      name: gateway-helm
      url: oci://docker.io/envoyproxy/gateway-helm
      version: "1.7.0"
    namespace: envoy-gateway-system
    values:
      config:
        envoyGateway:
          extensionApis:
            enableBackend: true
          extensionManager:
            backendResources:
            - group: inference.networking.k8s.io
              kind: InferencePool
              version: v1
            hooks:
              xdsTranslator:
                post:
                - Translation
                - Cluster
                - Route
                translation:
                  cluster:
                    includeAll: true
                  listener:
                    includeAll: true
                  route:
                    includeAll: true
                  secret:
                    includeAll: true
            service:
              fqdn:
                hostname: ai-gateway-controller.envoy-ai-gateway-system.svc.cluster.local
                port: 1063
  providerConfigRef:
    kind: ProviderConfig
    name: (join('-', ['a-team', $cluster]))
  managementPolicies:
  - Create
  - Update
  - Observe
  rollbackLimit: 3
---
apiVersion: kubernetes.m.crossplane.io/v1alpha1
kind: Object
metadata:
  annotations:
    crossplane.io/composition-resource-name: (join('-', ['a-team', $cluster, 'app-envoy-gateway-class']))
    crossplane.io/external-name: envoy-gateway-class
  name: (join('-', ['a-team', $cluster, 'app-envoy-gateway-class']))
  ownerReferences:
  - apiVersion: devopstoolkit.ai/v2
    blockOwnerDeletion: true
    controller: true
    kind: Cluster
spec:
  forProvider:
    manifest:
      apiVersion: gateway.networking.k8s.io/v1
      kind: GatewayClass
      metadata:
        name: eg
      spec:
        controllerName: gateway.envoyproxy.io/gatewayclass-controller
  providerConfigRef:
    kind: ProviderConfig
    name: (join('-', ['a-team', $cluster]))
---
apiVersion: kubernetes.m.crossplane.io/v1alpha1
kind: Object
metadata:
  annotations:
    crossplane.io/composition-resource-name: (join('-', ['a-team', $cluster, 'app-envoy-gateway']))
    crossplane.io/external-name: envoy-gateway
  name: (join('-', ['a-team', $cluster, 'app-envoy-gateway']))
  ownerReferences:
  - apiVersion: devopstoolkit.ai/v2
    blockOwnerDeletion: true
    controller: true
    kind: Cluster
spec:
  forProvider:
    manifest:
      apiVersion: gateway.networking.k8s.io/v1
      kind: Gateway
      metadata:
        name: eg
        namespace: envoy-gateway-system
      spec:
        gatewayClassName: eg
        listeners:
        - name: http
          protocol: HTTP
          port: 80
          allowedRoutes:
            namespaces:
              from: All
  providerConfigRef:
    kind: ProviderConfig
    name: (join('-', ['a-team', $cluster]))
---
apiVersion: kubernetes.m.crossplane.io/v1alpha1
kind: Object
metadata:
  annotations:
    crossplane.io/composition-resource-name: (join('-', ['a-team', $cluster, 'app-inference-crd-inferencepools']))
    crossplane.io/external-name: inferencepools.inference.networking.k8s.io
  name: (join('-', ['a-team', $cluster, 'app-inference-crd-inferencepools']))
  ownerReferences:
  - apiVersion: devopstoolkit.ai/v2
    blockOwnerDeletion: true
    controller: true
    kind: Cluster
spec:
  forProvider:
    manifest:
      apiVersion: apiextensions.k8s.io/v1
      kind: CustomResourceDefinition
      metadata:
        name: inferencepools.inference.networking.k8s.io
      spec:
        group: inference.networking.k8s.io
        names:
          kind: InferencePool
          listKind: InferencePoolList
          plural: inferencepools
          singular: inferencepool
        scope: Namespaced
        versions:
        - name: v1
          served: true
          storage: true
  managementPolicies:
  - Create
  - Update
  - Observe
  providerConfigRef:
    kind: ProviderConfig
    name: (join('-', ['a-team', $cluster]))
---
apiVersion: kubernetes.m.crossplane.io/v1alpha1
kind: Object
metadata:
  annotations:
    crossplane.io/composition-resource-name: (join('-', ['a-team', $cluster, 'app-inference-crd-inferenceobjectives']))
    crossplane.io/external-name: inferenceobjectives.inference.networking.x-k8s.io
  name: (join('-', ['a-team', $cluster, 'app-inference-crd-inferenceobjectives']))
  ownerReferences:
  - apiVersion: devopstoolkit.ai/v2
    blockOwnerDeletion: true
    controller: true
    kind: Cluster
spec:
  forProvider:
    manifest:
      apiVersion: apiextensions.k8s.io/v1
      kind: CustomResourceDefinition
      metadata:
        name: inferenceobjectives.inference.networking.x-k8s.io
      spec:
        group: inference.networking.x-k8s.io
        names:
          kind: InferenceObjective
          listKind: InferenceObjectiveList
          plural: inferenceobjectives
          singular: inferenceobjective
        scope: Namespaced
        versions:
        - name: v1alpha2
          served: true
          storage: true
  managementPolicies:
  - Create
  - Update
  - Observe
  providerConfigRef:
    kind: ProviderConfig
    name: (join('-', ['a-team', $cluster]))
---
apiVersion: helm.m.crossplane.io/v1beta1
kind: Release
metadata:
  annotations:
    crossplane.io/composition-resource-name: (join('-', ['a-team', $cluster, 'app-ai-gateway-crds-helm']))
    crossplane.io/external-name: ai-gateway-crds-helm
  name: (join('-', ['a-team', $cluster, 'app-ai-gateway-crds-helm']))
  ownerReferences:
  - apiVersion: devopstoolkit.ai/v2
    blockOwnerDeletion: true
    controller: true
    kind: Cluster
spec:
  forProvider:
    chart:
      name: ai-gateway-crds-helm
      url: oci://docker.io/envoyproxy/ai-gateway-crds-helm
      version: "0.5.0"
    namespace: envoy-ai-gateway-system
  providerConfigRef:
    kind: ProviderConfig
    name: (join('-', ['a-team', $cluster]))
  managementPolicies:
  - Create
  - Update
  - Observe
  rollbackLimit: 3
---
apiVersion: helm.m.crossplane.io/v1beta1
kind: Release
metadata:
  annotations:
    crossplane.io/composition-resource-name: (join('-', ['a-team', $cluster, 'app-ai-gateway-helm']))
    crossplane.io/external-name: ai-gateway-helm
  name: (join('-', ['a-team', $cluster, 'app-ai-gateway-helm']))
  ownerReferences:
  - apiVersion: devopstoolkit.ai/v2
    blockOwnerDeletion: true
    controller: true
    kind: Cluster
spec:
  forProvider:
    chart:
      name: ai-gateway-helm
      url: oci://docker.io/envoyproxy/ai-gateway-helm
      version: "0.5.0"
    namespace: envoy-ai-gateway-system
  providerConfigRef:
    kind: ProviderConfig
    name: (join('-', ['a-team', $cluster]))
  managementPolicies:
  - Create
  - Update
  - Observe
  rollbackLimit: 3
